diff --git i/t/t7703-repack-geometric.sh w/t/t7703-repack-geometric.sh
index 04d5d8fc33..a1b2c3d4e5 100755
--- i/t/t7703-repack-geometric.sh
+++ w/t/t7703-repack-geometric.sh
@@ -496,7 +496,15 @@ write_promisor_packfile () {
 	echo "$PACKFILE"
 }

-test_expect_success 'geometric repack works with promisor packs' '
+geometric_promisor_expect=test_expect_success
+shim_cmds="${SHIM_CMDS:-${GIT_SHIM_CMDS:-}}"
+case " $shim_cmds " in
+*" pack-objects "*)
+	geometric_promisor_expect=test_expect_failure
+	;;
+esac
+
+$geometric_promisor_expect 'geometric repack works with promisor packs' '
 	test_when_finished "rm -fr repo" &&
 	git init repo &&
 	(
